import { StyleSheet } from "react-native";

export const getStyles = () => StyleSheet.create({
    container: {
        flex: 1,
        alignItems: 'center',
        justifyContent: 'center',
    },
    stepFigure: {
        position: 'absolute',
    }
})